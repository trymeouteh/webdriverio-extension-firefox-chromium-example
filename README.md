A simple example of how to load extensions into firefox browser and chromium browser for webdriverio tests and be able to test the pages from the extension.

# Setup

``
git clone https://github.com/trymeouteh/webdriverio-extension-firefox-chromium-example
cd webdriverio-extension-firefox-chromium-example
npm i
npm run webdriverio
```
